import COINS from './COINS.js';

const COIN_ID = [
    `vending-machine-coin-${COINS[0]}-quantity`,
    `vending-machine-coin-${COINS[1]}-quantity`,
    `vending-machine-coin-${COINS[2]}-quantity`,
    `vending-machine-coin-${COINS[3]}-quantity`,
];

export default COIN_ID;
