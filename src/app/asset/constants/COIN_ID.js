import COIN from './COIN.js';

const COIN_ID = [
    `vending-machine-coin-${COIN[0]}-quantity`,
    `vending-machine-coin-${COIN[1]}-quantity`,
    `vending-machine-coin-${COIN[2]}-quantity`,
    `vending-machine-coin-${COIN[3]}-quantity`,
];

export default COIN_ID;
