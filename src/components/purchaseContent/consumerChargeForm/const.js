const ID_PREFIX = 'charge';

export const ID_FORM = `${ID_PREFIX}-form`;
export const DICT_PROPS_INPUT = {
  id: `${ID_PREFIX}-input`,
  type: 'number',
  placeholder: '투입할 금액',
};

export const DICT_PROPS_BUTTON = {
  id: `${ID_PREFIX}-button`,
  innerText: '투입하기',
};
