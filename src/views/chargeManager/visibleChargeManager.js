import VisibleManager from "../common/visibleManager.js";

class VisibleChargeManager extends VisibleManager {
  constructor() {
    super("charge-manager");
  }
}

export default VisibleChargeManager;
