import VisibleManager from "../common/visibleManager.js";

class VisiblePurchaseManager extends VisibleManager {
  constructor() {
    super("purchase-manager");
  }
}

export default VisiblePurchaseManager;
