import VisibleManager from "../common/visibleManager.js";

class VisibleProductManager extends VisibleManager {
  constructor() {
    super("product-manager");
  }
}

export default VisibleProductManager;
